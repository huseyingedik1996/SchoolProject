@model StudenthasMajorClassCreateModel
@{
    ViewData["Title"] = "CreateShasMC";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="oppositeFormTable" class="col-md-12">
    <form asp-action="CreateShasMC" method="post">
        <div class="card-header">
            <div class="card-title">Sınıf/Bölüm Seç</div>
        </div>

        <div class="form-group">

            <label for="MajorhasClassesId">Sınıf Seçin:</label>
            <select asp-for="MajorhasClassesId" class="form-control" asp-items="@(new SelectList(ViewBag.MC, "Id", "CombinedName"))">
            </select>
            <span asp-validation-for="MajorhasClassesId" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary">Seç</button>
    </form>
</div>

